var col = {
  col1: 0,
  col2: 0,
  col3: 0,
};

function setup() {
  createCanvas(500, 500);
  background(0, 0, 0);
}

function draw() {
  for(var i = 0; i <= 1000; i++) {
    console.log(1);
  };
};